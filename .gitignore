# NPM / Next.js
node_modules
.next
out
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo

# Jekyll (legacy, remove after migration)
_site
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
**/Gemfile.lock

# Temporary files
tmp
.DS_Store
Desktop.ini
._*
Thumbs.db
ehthumbs.db
.Spotlight-V100
.Trashes

# Env and secrets
.env
.env.local
.env*.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key

# IDE and editor (local workspace — do not publish)
.cursor/
.vscode/
.idea/

# Internal / operational docs (infra, DNS, redirect rules)
docs/

# Template / tooling metadata (not part of the site)
builtatlightspeed.json

# Deploy platforms (local config only)
.vercel

# Test coverage
coverage/
